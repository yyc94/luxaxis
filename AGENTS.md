# Agent Instructions

Read `CONTEXT.md` before changing domain language.

Read `docs/HYPRLAND_PLUGIN.md` in full before work involving the Hyprland
plugin, wallpaper ownership, workspace profiles, Spotlight effects,
transitions, rendering, configuration, or Phase 2 tests. Treat that document
as the accepted product and implementation specification.

Keep the existing Noctalia plugin independently usable. The Hyprland plugin is
a separate build and runtime deliverable in the same repository.
